{
  "name": "capitovo-proxy",
  "version": "0.1.0",
  "description": "Simple proxy for Alpha Vantage with caching for capitovo",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
{
  "name": "capitovo-fmp-proxy",
  "version": "0.1.0",
  "description": "Simple proxy to fetch FinancialModelingPrep data and keep API key server-side",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "NODE_ENV=development node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  }
}
